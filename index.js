const quickSort = array => {
  return array
}

const toFlat = array => {
  return array
}

const binarySearch = (array, searchValue) => {
  return 0
}

class HashTable {
  constructor () {
    this.map = {}
  }
  add (value) {}
  find (name) {}
}

module.exports = {
  quickSort,
  toFlat,
  binarySearch,
  HashTable,
}
